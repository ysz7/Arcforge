/**
 * Legacy path to icon.png (used only if needed for static references).
 * In-app icon is AppIcon component (SVG with currentColor) for theme adaptation.
 */
export const APP_ICON_PATH = 'icon.png';
