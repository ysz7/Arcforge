/**
 * ArcSpec node type definitions in the standard plugin format.
 * Exported as a plain array consumed by loadManifestPlugin() in the main process.
 */

import { ARCSPEC_NODE_TYPES } from './nodeTypes';

module.exports = ARCSPEC_NODE_TYPES;
